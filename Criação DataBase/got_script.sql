CREATE DATABASE GOT;
USE GOT;

CREATE TABLE `PERSONAGENS` (
  `PERSONAGEM` varchar(35) DEFAULT NULL,
  `ATOR_PRINCIPAL` varchar(30) PRIMARY KEY NOT NULL,
  `EPISODIOS_QUE_APARECEU` int DEFAULT NULL,
  `PRIMEIRA_APARICAO` int DEFAULT NULL,
  `ULTIMA_APARICAO` int DEFAULT NULL
);

CREATE TABLE `EPISODIOS` (
  `TEMPORADA` int DEFAULT NULL,
  `EPISODIO` int DEFAULT NULL,
  `TITULO` varchar(50) DEFAULT NULL,
  `DATA_LANÇAMENTO` varchar(10),
  `NOTA` float DEFAULT NULL,
  `VOTOS` int DEFAULT NULL,
  `SUMARIO` text DEFAULT NULL,
  `ESCRITOR1` varchar(20) DEFAULT NULL,
  `ESCRITOR2` varchar(20) DEFAULT NULL,
  `PRINCIPAL1` varchar(25) DEFAULT NULL,
  `PRINCIPAL2` varchar(25) DEFAULT NULL,
  `PRINCIPAL3` varchar(25) DEFAULT NULL,
  `AVALIACOES_USUARIOS` int DEFAULT NULL,
  `AVALIACOES_CRITICOS` int DEFAULT NULL,
  `US_VIEWERS` float DEFAULT NULL,
  `DURACAO` int DEFAULT NULL,
  `DIRETOR` varchar(20) DEFAULT NULL,
  CONSTRAINT `fk_ATOR_PRINCIPAL_ID` FOREIGN KEY (`PRINCIPAL1`) REFERENCES `PERSONAGENS` (`ATOR_PRINCIPAL`),
  FOREIGN KEY (`PRINCIPAL2`) REFERENCES `PERSONAGENS` (`ATOR_PRINCIPAL`),
  FOREIGN KEY (`PRINCIPAL3`) REFERENCES `PERSONAGENS` (`ATOR_PRINCIPAL`)
);

CREATE TABLE `CASAS` (
  `NOME_CASA` varchar(35) DEFAULT NULL,
  `REGIAO` varchar(45) DEFAULT NULL
);


